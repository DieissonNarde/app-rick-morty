import { BrowserRouter, Route, Switch } from 'react-router-dom';
import GlobalStyle from './styles/global';

function App() {
  return (
    <BrowserRouter>
      <Switch>
      </Switch>
      <GlobalStyle />
    </BrowserRouter>
  );
}

export default App;